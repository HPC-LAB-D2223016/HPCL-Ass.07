#include <omp.h>
#include <stdio.h>

int main() {
    int n = 16, i;
    #pragma omp parallel for schedule(static, 4)
    for (i = 0; i < n; i++) {
        printf("Thread %d processing iteration %d\n", omp_get_thread_num(), i);
    }
    return 0;
}
